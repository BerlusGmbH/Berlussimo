#import "./PropertyBaseFragment.graphql"

fragment PropertyCopiedNotificationBase on PropertyCopiedNotification {
    __typename
    id
    source {
        ...PropertyBase
    }
    target {
        ...PropertyBase
    }
}