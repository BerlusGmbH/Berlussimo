#import "../../graphql/DetailBaseFragment.graphql"

query PropertyEMails($id: ID!) {
    property(id : $id) {
        id
        __typename
        tenantsEMails {
            ...DetailBase
        }
        homeOwnersEMails {
            ...DetailBase
        }
    }
}
