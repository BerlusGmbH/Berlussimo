#import "../../../../../graphql/CredentialBaseFragment.graphql"

query Credential($personId: ID!) {
    person(id: $personId) {
        id
        credential {
            ...CredentialBase
        }
    }
}