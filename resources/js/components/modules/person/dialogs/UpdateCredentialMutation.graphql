#import "../../../../../graphql/CredentialBaseFragment.graphql"

mutation updateCredential($personId: ID!, $enabled: Boolean, $password: String) {
    updateCredential(personId: $personId, enabled: $enabled, password: $password) {
        id
        credential {
            ...CredentialBase
        }
    }
}